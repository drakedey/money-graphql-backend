type Query {
  transactionsByAccount(accountId: Int): [Transaction]
  transactionsByUser: [Transaction]
}

type Mutation {
  createTransaction(ammount: Int, type: TransactionType, accountId: Int): Transaction
}
